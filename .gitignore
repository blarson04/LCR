# Secrets — never commit real API keys
.env

# Machine-local Claude Code settings (personal, not shared)
.claude/settings.local.json
.claude/launch.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Cached raw downloads (reproducible from code; large/binary)
data/raw/*
!data/raw/.gitkeep

# Jupyter
.ipynb_checkpoints/

# OS / editor cruft
.DS_Store
Thumbs.db

# Office lock/temp files (this project lives in OneDrive)
~$*

# Local critique/upload packages (derived, not source)
critique/
decision-log.docx

# Session handoff notes (local operational doc)
HANDOFF.md

# Third-party report (copyrighted; reference only, never publish)
Arbor-Report*.pdf
