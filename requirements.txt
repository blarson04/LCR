# Deployed-app runtime ONLY (see requirements-pipeline.txt for the data pipeline).
#
# streamlit & plotly are PURE-PYTHON (one wheel for all Pythons), so we cap them
# below the versions whose API changes broke the rendered app — no wheel risk.
# pandas/numpy/pyarrow are COMPILED, so they stay OPEN floors (no upper cap):
# capping them forced Streamlit Cloud into a failing source build when the Cloud
# Python only had wheels for newer versions.

streamlit>=1.40,<1.49
plotly>=5.18,<6
pandas>=2.1
numpy>=1.26
pyarrow>=14
