# Deployed-app runtime ONLY — what app/streamlit_app.py imports (via
# src/score -> normalize -> indicators -> config).
#
# Version FLOORS, not exact pins: exact pins forced Streamlit Cloud to compile
# pandas from source (a .tar.gz) when its Python version had no matching wheel,
# which stalled the build. Floors let pip grab a prebuilt wheel for whatever
# Python Cloud runs. The data is precomputed and committed, so minor library
# version drift here doesn't change the app's output.
#
# Exact pins for reproducing the data pipeline live in requirements-pipeline.txt.

streamlit>=1.40
plotly>=5.20
pandas>=2.1
numpy>=1.26
pyarrow>=14
