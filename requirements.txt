# Deployed-app runtime ONLY (see requirements-pipeline.txt for the data pipeline).
#
# streamlit & plotly are PINNED to the tested versions: they're pure-Python
# wheels (no Python-version wheel risk), and unpinned floors let Streamlit Cloud
# install a newer Streamlit whose API changes broke the app (e.g. use_container_width).
# pandas/numpy/pyarrow are COMPILED, so they stay as capped ranges — floors ensure
# a prebuilt wheel exists for the Cloud Python, caps avoid breaking major releases.

streamlit==1.41.1
plotly==5.24.1
pandas>=2.1,<2.4
numpy>=1.26,<2.4
pyarrow>=16,<19
