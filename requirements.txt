# Deployed-app runtime ONLY — exactly what app/streamlit_app.py imports
# (directly or via src/score -> normalize -> indicators -> config). Kept lean so
# the Streamlit Cloud build is fast and has few failure points.
#
# To regenerate the DATA PIPELINE (ingest/panel/score/backtest/registry) you need
# the fuller toolchain in requirements-pipeline.txt:
#     pip install -r requirements-pipeline.txt

streamlit==1.41.1
plotly==5.24.1
pandas==2.2.3
numpy==2.2.1
pyarrow==18.1.0          # read the processed parquet panel/scores
